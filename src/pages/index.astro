---
const brand = "Berrystock Solutions";
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Berrystock Solutions</title>

  <style>
    :root {
      --bg: #070707;
      --panel: #111111;
      --text: #f5f5f5;
      --muted: #9a9a9a;
      --accent: #ffffff;
      --line: rgba(255,255,255,0.08);
      --max: 1100px;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
    }

    .container {
      max-width: var(--max);
      margin: 0 auto;
      padding: 0 20px;
    }

    /* NAV */

    nav {
      position: sticky;
      top: 0;
      background: rgba(7,7,7,0.9);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--line);
      z-index: 10;
    }

    nav .inner {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 16px 0;
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
      color: var(--text);
    }

    .brand img {
      width: 34px;
      height: 34px;
      border-radius: 8px;
      filter: grayscale(1) contrast(1.1);
    }

    .brand span {
      font-weight: 600;
      letter-spacing: 0.02em;
    }

    nav a {
      color: var(--muted);
      margin-left: 22px;
      font-size: 14px;
      text-decoration: none;
    }

    nav a:hover { color: var(--text); }

    /* HERO */

    .hero {
      padding: 110px 0 80px;
    }

    h1 {
      font-size: 48px;
      line-height: 1.2;
      margin-bottom: 20px;
      letter-spacing: -0.02em;
      max-width: 800px;
    }

    p.lead {
      color: var(--muted);
      font-size: 18px;
      max-width: 700px;
    }

    .cta {
      margin-top: 32px;
      display: inline-block;
      padding: 14px 22px;
      border-radius: 10px;
      background: var(--accent);
      color: #000;
      font-weight: 600;
      text-decoration: none;
    }

    .cta:hover {
      opacity: 0.9;
    }

    /* SECTIONS */

    .section {
      padding: 80px 0;
      border-top: 1px solid var(--line);
    }

    h2 {
      font-size: 30px;
      margin-bottom: 28px;
      letter-spacing: -0.01em;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 24px;
    }

    .card {
      background: var(--panel);
      padding: 24px;
      border-radius: 16px;
      border: 1px solid var(--line);
      transition: border 0.2s ease;
    }

    .card:hover {
      border-color: rgba(255,255,255,0.2);
    }

    .card h3 {
      margin-top: 0;
      margin-bottom: 12px;
      font-size: 18px;
    }

    .card p {
      color: var(--muted);
      font-size: 15px;
    }

    footer {
      padding: 60px 0;
      color: var(--muted);
      font-size: 14px;
      border-top: 1px solid var(--line);
      margin-top: 60px;
      text-align: center;
    }

    @media (max-width: 768px) {
      h1 { font-size: 36px; }
    }
  </style>
</head>

<body>

<nav>
  <div class="container inner">
    <a href="/" class="brand">
      <img src="/assets/brand-mark.jpg" alt="Berrystock Solutions" />
      <span>{brand}</span>
    </a>
    <div>
      <a href="#services">Services</a>
      <a href="#work">Work</a>
      <a href="#start">Start</a>
    </div>
  </div>
</nav>

<section class="hero container">
  <h1>Secure, scalable cloud platforms and business systems integration.</h1>
  <p class="lead">
    I design and operate modern cloud platforms and integrate the business systems
    that depend on them — from infrastructure foundations to automation and AI workflows.
  </p>
  <a class="cta" href="#start">Start your project</a>
</section>

<section id="services" class="section container">
  <h2>What I Do</h2>
  <div class="grid">
    <div class="card">
      <h3>Platform Architecture</h3>
      <p>Structured, secure cloud environments built for scale, reliability, and operational clarity.</p>
    </div>

    <div class="card">
      <h3>Systems Integration</h3>
      <p>ERP, CRM, telephony and automation platforms connected into cohesive business workflows.</p>
    </div>

    <div class="card">
      <h3>Cloud Optimisation</h3>
      <p>Security hardening, cost efficiency and operational refinement across cloud environments.</p>
    </div>
  </div>
</section>

<section id="work" class="section container">
  <h2>Selected Work</h2>
  <div class="grid">
    <div class="card">
      <h3>AI Enquiry Automation Platform</h3>
      <p>Serverless enquiry capture integrated with automation workflows and structured data logging.</p>
    </div>

    <div class="card">
      <h3>Cloud ERP Deployment</h3>
      <p>Secure Linux-based ERP deployment with HTTPS, reverse proxy, and controlled access patterns.</p>
    </div>

    <div class="card">
      <h3>Secure Admin Access Model</h3>
      <p>Identity-first access control patterns for self-hosted systems without exposing management interfaces publicly.</p>
    </div>
  </div>
</section>

<section id="start" class="section container">
  <h2>Work With Me</h2>
  <p class="lead">
    If you're building, restructuring, or stabilising your cloud platform — let's talk.
  </p>

  <a class="cta" href="mailto:enquiries@berrystocksolutions.co.uk">
    enquiries@berrystocksolutions.co.uk
  </a>
</section>

<footer class="container">
  © {new Date().getFullYear()} Berrystock Solutions
</footer>

</body>
</html>
